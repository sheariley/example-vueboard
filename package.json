{
  "name": "vueboard",
  "scripts": {
    "db:mock": "json-server ./mock-data/db.json -w -c ./mock-data/json-server-config.json",
    "db:start": "npx supabase start -x vector",
    "db:stop": "npx supabase stop"
  },
  "dependencies": {
    "json-server": "0.17.4",
    "supabase": "^2.58.5"
  }
}
